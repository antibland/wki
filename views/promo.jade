extends layout

block content
  section
    .inner
      if(typeof msg !== 'undefined')

        div(class='message', id='dismiss-message')
          a(href='#dismiss-message', aria-label='Click or touch to dismiss message')
            p= msg

      h1=header_text
      h2 Add a promotion

      form(method='POST' action='/promo')
        label(for="title") Promotion title
        input(type="text" name="title" id="title" required placeholder="My Awesome Promotion")
        label(for="text_teaser") Promotion teaser text
        input(type="text" name="text_teaser" id="text_teaser" required placeholder="I appear inside the promotion popup. Keep me small.")
        label(for="text") Promotion full text
        textarea(name="text" id="text" required)
        input(type="submit" class="button" value="Submit")

      h2 Update or delete a promotion

      if promos.length

        each promo, index in promos
          form(method="POST" action="/promo/#{promo._id}/update")
            ul.promos
              li
                span.label Title
                input(type="text" name="update_title_#{promo._id}" value="#{promo.title}" required)
              li
                span.label Promotion teaser text
                input(type="text" name="update_text_teaser_#{promo._id}" value="#{promo.text_teaser}" required)
              li
                span.label Promotion full text
                textarea(name="update_text_#{promo._id}" required)=promo.text
              li
                input.button(type="submit" value="Update")
                a.delete(href='/promo/#{promo._id}/delete') Delete
      else
        p There are no promotions so far.



        //- select(name="promos" id="promos")
        //-   option(value="#") --
        //-   for promo in promos
        //-     option(value="#{promo.title}") #{promo.title} - #{promo.date}


